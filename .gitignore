# Dependencies
node_modules/

# Astro build output
site/dist/
site/.astro/

# Local Claude settings (personal/local — but keep .claude/skills/ tracked)
.claude/settings.local.json

# Env / secrets
.env
.env.*

# Python venv for ESP32 tooling (machine-specific, regenerable)
.esp-venv/

# PlatformIO build cache + libs (regenerable via `pio run`)
esp32-firmware/.pio/

# Logs
*.log

# OS cruft
.DS_Store
Thumbs.db
